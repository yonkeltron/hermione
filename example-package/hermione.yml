---
name: "Example Package"
author: "J. Random Hacker"
description: "My awesome example package"
id: org.hermione.example-package
mappings:
  - i: a.txt
    o: "{{HOME}}/panda.txt"
    platform: windows
  - i: b.txt
    o: "{{HOME}}/bamboo.txt"
hooks:
  post_install: |
    echo Hello from post_install hook
  pre_install: |
    echo Hello from pre_install hook
  pre_remove: |
    echo Hello from pre_remove hook
  post_remove: |
    echo Hello from post_remove hook
